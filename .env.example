APP_SESSION_SECRET=replace-with-a-long-random-string

# Optional SMTP settings for real contact-form delivery.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=

# Optional overrides.
DATABASE_PATH=./data/app.db
UPLOADS_DIR=./uploads
PUBLIC_BASE_URL=
